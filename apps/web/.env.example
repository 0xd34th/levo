# Database
DATABASE_URL="postgresql://levo:levo_dev@localhost:5432/levo"

# Redis
REDIS_URL="redis://localhost:6379"

# twitterapi.io
TWITTER_API_KEY=""

# Privy (X/Twitter login)
NEXT_PUBLIC_PRIVY_APP_ID=""
PRIVY_APP_SECRET=""

# HMAC secret for quote tokens (generate with: openssl rand -hex 32)
HMAC_SECRET=""
APP_ORIGIN="http://localhost:3000"

# Sui
NEXT_PUBLIC_SUI_NETWORK="testnet"
NEXT_PUBLIC_PACKAGE_ID=""
NEXT_PUBLIC_VAULT_REGISTRY_ID=""
NEXT_PUBLIC_TREASURY_CAP_ID=""

# Gas station (optional — base64-encoded 32-byte Ed25519 seed)
# Generate with: openssl rand -base64 32
# Fund the derived address with SUI to sponsor gas for embedded wallet transactions
GAS_STATION_SECRET_KEY=""

# Nautilus enclave (required for claim flow)
ENCLAVE_REGISTRY_ID=""
NAUTILUS_ENCLAVE_URL=""
